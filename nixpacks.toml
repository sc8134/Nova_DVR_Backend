[phases.setup]
nixPkgs = ['ffmpeg', 'python311']

[phases.install]
cmds = ['pip install -r requirements.txt']

[start]
cmd = 'gunicorn app:app --bind 0.0.0.0:\ --timeout 300 --workers 2 --threads 4 --worker-class gthread'
